# This project builds with yarn (see ../reinstall.sh and
# .github/workflows/js-bundle-check.yaml); yarn.lock is the only lockfile.
# Prevent an accidental `npm install` from creating a package-lock.json.
package-lock=false
